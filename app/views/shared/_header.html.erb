<%# CSS・・・assets/stylesheets/shared/header.css %>
  <header class='index-header d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4'>
          <h1 class="header-title">H.R</h1>
          <div class="text-end">
            <% if user_signed_in? %>
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-primary me-2 logout" %>
            <% else %>
              <%= link_to "新規登録", new_user_registration_path, class: "btn btn-outline-primary me-2" %>
              <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
            <% end %>
          </div>
  </header>
